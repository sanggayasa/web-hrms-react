// import ColomTb from "./colom-tb";

function BodyTb({data}){
    // console.log(data);
    return (
        <tbody>
            {
                //  data.map(data=><tr key={data.name}>{data.name}</tr>)
            }
        </tbody>
    )
}

export default BodyTb;